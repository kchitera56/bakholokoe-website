// small helper - you can extend this later
document.addEventListener('DOMContentLoaded', function(){
  // maybe fetch /api/recommend later
});

function keepSignedInToggle(e){
  // handled server-side via 'remember' checkbox
}
